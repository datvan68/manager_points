Task: activities-attendance-table-improvements | feature_development | Risk: medium | Profile: Quick
Objective: Improve the activity-attendance table with reusable 40-row pagination, selected-row Excel export, class names, and colored Vietnamese attendance labels.
Boundary: Frontend attendance page/tests; preserve API, authorization, backend behavior, and unrelated screens | Write: `frontend/src/app/(dashboard)/activities/attendance/page.tsx`, `frontend/src/app/(dashboard)/activities/attendance/page.test.tsx`
Targets: `ActivitiesAttendancePage`; existing `CustomPagination`, `FloatingActionBar`, `ResponsiveDataView`, `activityAttendanceApi.getAll`, and `xlsx` dependency.
Steps: Baseline API/rendering -> replace manual paging with `CustomPagination`, page size `40`, options including `40`, and page reset on size/filter change -> add row/current-page select-all checkboxes and indeterminate state -> show `FloatingActionBar` for selections and export exactly selected records to `.xlsx` -> render `class_id.class_name`/`name` -> map `present`/`absent` to styled `Có mặt`/`Vắng mặt`, with safe Vietnamese fallbacks -> add UI tests.
Verify: `D:\PROJECT\manager_points\frontend :: npm test -- "src/app/(dashboard)/activities/attendance/page.test.tsx"` => pagination, selection/export, class, and status cases pass; `npm run typecheck` => no TypeScript errors; repository root :: `git diff --check` => clean.
Done: Default request/page size is 40; reusable pagination controls page/size; selecting rows opens the bar and export contains only those rows; class names and colored `Có mặt`/`Vắng mặt` render correctly; loading, empty, filter, and permission behavior remain intact.
Gate: None
