Task: fix-activity-club-type-build | bug_fix | Risk: medium | Profile: Quick
Objective: Frontend TypeScript and Next.js builds accept club activities while preserving the existing attendance context mapping (`club` for clubs, `activity` for other activity types).
Boundary: `frontend/**` | Write: `frontend/src/api/activity-api.ts` and a focused existing frontend test only if regression coverage requires it
Targets: `Activity.activity_type` in `frontend/src/api/activity-api.ts`; the comparison and `useAttendanceSession` call in `frontend/src/app/(dashboard)/activities/[activityId]/page.tsx`; related tests in `frontend/src/app/(dashboard)/activities/[activityId]/page.test.tsx`
Steps: Confirm the API type is the source of the failed comparison -> replace the duplicated `'activity'` literal with `'club'` in the canonical `Activity` union, without adding casts or changing runtime behavior -> add/update the smallest useful regression assertion only if existing coverage does not protect the mapping -> run focused test, typecheck, build, and inspect the final diff/status while preserving unrelated working-tree changes.
Verify: `frontend` :: `npm test -- "src/app/(dashboard)/activities/[activityId]/page.test.tsx"` => focused activity detail tests pass; `frontend` :: `npm run typecheck` => no TypeScript errors; `frontend` :: `npm run build` => Next.js production build completes successfully; repository root :: `git diff --check -- frontend/src/api/activity-api.ts "frontend/src/app/(dashboard)/activities/[activityId]/page.test.tsx"` => no whitespace errors.
Done: `Activity.activity_type` includes `'club'` exactly once and retains `'activity'`, `'event'`, and `'festival'`; the reported TS2367 comparison error is absent; club/non-club attendance context behavior remains unchanged; all required verification passes with no unrelated files modified by this task.
Gate: None
