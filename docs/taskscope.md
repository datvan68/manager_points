Task: frontend-not-found-and-login-typography | bug_fix | Risk: medium | Profile: Quick
Objective: Give unmatched frontend URLs a clear branded 404 recovery screen and make login typography visibly lighter and softer while preserving readability.
Boundary: Frontend App Router presentation only; preserve all valid routes, authentication/submission/redirect behavior, validation, responsive layout, colors, controls, and backend APIs | Write: `frontend/src/app/not-found.tsx`, `frontend/src/app/(auth)/login/page.tsx`, `frontend/src/app/auth-pages-ui.test.tsx`
Targets: global App Router `not-found` boundary; `LoginPage` heading, subtitle, field values/placeholders, remember label, trust badge, and submit-label typography shown in the supplied reference.
Steps: Baseline unmatched-route and login rendering -> add an accessible Vietnamese 404 screen with a safe recovery link to `/` -> reduce excessive login font weights and soften supporting-text colors without weakening labels, focus states, contrast, or loading/error states -> add focused rendering/navigation and typography regression tests -> review responsive desktop/mobile output and scoped diff.
Verify: `D:\PROJECT\manager_points\frontend :: npm test -- "src/app/auth-pages-ui.test.tsx"` => 404 recovery and login typography cases pass; `npm run typecheck` => no TypeScript errors; `npm run build` => App Router builds the not-found boundary and existing routes successfully; repository root :: `git diff --check` => clean.
Done: Unknown frontend paths render the custom 404 screen and its recovery action reaches `/`; valid routes remain available; login text is lighter/less visually dense at mobile and desktop sizes, with unchanged form behavior and accessible contrast.
Gate: None
