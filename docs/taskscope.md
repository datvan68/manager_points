Task: `fix-mobile-dashboard-shell-and-bottom-nav-spacing` | `bug_fix` | Risk: medium | Profile: Quick
Objective: On mobile iOS, dragging dashboard content or interactive items never moves the header, while the bottom navigation uses tighter bottom, side, and active-state spacing.
Boundary: `frontend/src/**` | Write: `frontend/src/globals.css`, `frontend/src/app/(dashboard)/layout.tsx`, `frontend/src/components/layout/Header.test.tsx`
Targets: `.dashboard-shell` viewport anchoring and overscroll containment; dashboard content flex/scroll boundary; `.mobile-bottom-nav` bottom offset and horizontal width; `.mobile-bottom-nav-item-active::before` dimensions; responsive shell contract test.
Steps: Capture the current mobile shell and bottom-navigation CSS contract -> anchor the dashboard shell to the viewport so iOS rubber-band gestures originating from `main`, buttons, or tab navigation cannot translate the header -> preserve page-owned vertical scrolling -> reduce bottom and horizontal navigation margins plus active-indicator padding -> update the focused regression contract -> run focused tests, type checking, and diff review.
Verify: `D:\PROJECT\manager_points\frontend :: npm test -- --run src/components/layout/Header.test.tsx` => mobile shell and navigation contract passes; `D:\PROJECT\manager_points\frontend :: npm run typecheck` => TypeScript passes; `D:\PROJECT\manager_points :: git diff --check` => patch formatting is valid.
Done: Header remains visually stationary for drag gestures starting anywhere in dashboard content; main content remains scrollable; mobile bottom navigation has visibly smaller bottom and left/right gaps and a tighter active background; desktop layout remains unchanged.
Gate: None
