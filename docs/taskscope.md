Task: attendance-success-feedback-and-completion-award | bug_fix | Risk: MEDIUM
Objective: Show durable successful-attendance feedback to the member and create exactly one criterion record when the member reaches the configured number of verified attendances.
Scope:
- frontend/src/app/(dashboard)/activities/[activityId]/page.tsx :: member attendance controls :: identify the current member's own check-in, show one success toast after QR or GPS check-in, prevent another check-in for the same session, and change the QR/GPS action to the disabled `Đã điểm danh` state after local, initial-load, or realtime confirmation.
- frontend/src/app/(dashboard)/activities/[activityId]/page.test.tsx :: attendance interaction regressions :: verify QR and GPS success toasts, persistent checked-in state, duplicate-action prevention, and that another member's roster entry does not mark a president as checked in.
- frontend/src/components/attendance/ProximityCheckinButton.tsx :: GPS success control :: preserve the current success explanation while presenting the completed action as a disabled `Đã điểm danh` button.
- frontend/src/components/attendance/ProximityCheckinButton.test.tsx :: GPS control state coverage :: verify the completed button label and disabled behavior without requesting geolocation or submitting another check-in.
- backend/src/attendance-sessions/attendance-sessions.module.ts :: attendance-to-record synchronization dependency :: make the existing activity-attendance synchronization service available to attendance sessions without duplicating completion-rule logic.
- backend/src/attendance-sessions/attendance-sessions.service.ts :: verified check-in synchronization :: sync QR/GPS check-ins for both `club` and `activity` contexts into ActivityAttendance and invoke the existing academic-record synchronization path immediately only when the attendance is approved.
- backend/src/attendance-sessions/attendance-sessions.service.spec.ts :: synchronization and approval regressions :: prove auto-approved QR/GPS attendance evaluates completion rules, pending attendance waits for manual approval, both supported contexts are handled, and retry or existing-record paths remain idempotent.
- docs/taskscope.md :: execution contract :: record the verified gaps, bounded changes, verification, and completion criteria for this fix.
Out: Completion-rule CRUD or scoring semantics, criterion configuration UI, manual attendance approval UI, QR token generation, GPS distance calculation, realtime event payload redesign, database migrations, dependency upgrades, and unrelated files and behavior.
Context: ActivityCompletionService already counts only `approval_status: approved` attendances with `status` equal to `present` or `late`, then creates an idempotent quantity-one AcademicRecord and ActivityCompletionAward per configured criterion. Manual approval already enters this path through ActivityAttendanceSyncService. AttendanceSessionsService currently bypasses that service after an auto-approved QR/GPS sync and only creates ActivityAttendance for `club`, so valid `activity` sessions and auto-approved check-ins may never evaluate the completion rule. Existing uncommitted attendance and realtime changes are user-owned and must be preserved.
Steps:
1. Pass the normalized current Student ID into the attendance tab and derive `hasCurrentMemberCheckedIn` from that ID and the hook's check-ins, with the local successful result as immediate confirmation.
2. After successful member-initiated QR or GPS submission, show one localized success toast, refresh the surrounding activity data, close the QR scanner as currently designed, and keep both attendance actions disabled and labeled `Đã điểm danh` for the remainder of the session and after reload/realtime updates.
3. Import and inject ActivityAttendanceSyncService into AttendanceSessionsService, retaining the existing ActivityAttendance persistence and error-isolation behavior.
4. Run ActivityAttendance synchronization for scheduled `club` and `activity` sessions; after an existing or newly created attendance becomes approved, call the existing synchronization service so the active completion rule is evaluated. Leave pending records for the existing manual approval flow.
5. Add focused frontend and backend regressions for success feedback, own-member state, approval gating, threshold handoff, supported contexts, and idempotency.
6. Run targeted checks and builds, then inspect only the scoped diff for unintended localization, encoding, or user-owned changes.
Verify:
- frontend :: npm test -- "src/app/(dashboard)/activities/[activityId]/page.test.tsx" "src/components/attendance/ProximityCheckinButton.test.tsx" => both targeted Vitest suites pass, including toast, completed-button, reload/realtime, and own-member cases.
- frontend :: npm run typecheck => TypeScript completes without errors.
- backend :: npm test -- --runInBand attendance-sessions.service.spec.ts => the targeted Jest suite passes, including approved, pending, club, activity, and idempotent synchronization cases.
- backend :: npm run test:e2e -- --runInBand activities.e2e-spec.ts => the existing completion-rule flow still awards one record only after the configured approved-attendance threshold and remains idempotent.
- backend :: npm run build => NestJS compiles with the added synchronization dependency.
- repository root :: git diff --check -- "frontend/src/app/(dashboard)/activities/[activityId]/page.tsx" "frontend/src/app/(dashboard)/activities/[activityId]/page.test.tsx" frontend/src/components/attendance/ProximityCheckinButton.tsx frontend/src/components/attendance/ProximityCheckinButton.test.tsx backend/src/attendance-sessions/attendance-sessions.module.ts backend/src/attendance-sessions/attendance-sessions.service.ts backend/src/attendance-sessions/attendance-sessions.service.spec.ts docs/taskscope.md => no whitespace errors in scoped files.
- repository root :: git status --short and scoped diff inspection => no unrelated or pre-existing user changes were overwritten.
Done:
- A member receives one success toast after a successful QR or GPS check-in, and the applicable control immediately and durably shows disabled `Đã điểm danh` without allowing a duplicate submission for that session.
- A manager who is also a member is marked checked in only by that manager's own Student ID, not by another member appearing in the roster.
- Only approved `present` or `late` attendance counts toward the active rule; pending, rejected, absent, or failed check-ins do not create a completion record.
- On reaching `minimum_attendance`, the system creates exactly one quantity-one AcademicRecord and one ActivityCompletionAward per configured criterion for the activity, student, and semester; later checks or retries create no duplicates.
- Targeted tests, completion-rule e2e coverage, frontend type-check, backend build, and scoped diff checks pass.
Gate/Stop: Stop if successful session check-ins cannot be mapped to ActivityAttendance without changing stored data, if ActivityAttendanceSyncService cannot be injected without a circular dependency or files outside Scope, if the completion rule is intended to award per attendance rather than once at the threshold, or if verification exposes a pre-existing failure that cannot be separated from this change.
Rollback: Revert only the scoped UI state, synchronization injection/calls, and regression-test hunks; no data rollback is required because idempotency keys and the completion-award unique index prevent duplicate awards.
Dependencies: Existing ActivityAttendanceModule exports ActivityAttendanceSyncService; existing ActivityCompletionService, AcademicRecord idempotency key, ActivityCompletionAward unique index, attendance realtime updates, and Sonner toast provider. No new package or migration.
Artifacts: Final scoped diff and successful targeted frontend/backend test, type-check, e2e, and build output.
