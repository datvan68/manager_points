Task: `fix-ios-focus-zoom-and-mobile-header-drag` | `bug_fix` | Risk: medium | Profile: Quick
Objective: On iOS mobile, focusing form controls does not trigger automatic page zoom, and the dashboard header remains anchored while users drag or scroll content.
Boundary: `frontend/src/**` | Write: `frontend/src/globals.css`, `frontend/src/components/layout/Header.tsx`, `frontend/src/components/layout/Header.test.tsx`
Targets: iOS/mobile rules for native `input`, `select`, and `textarea`; `.dashboard-shell` scroll/overscroll containment; conflicting `sticky`/`relative` positioning and mobile touch behavior on `.dashboard-header`; existing responsive shell contract test.
Steps: Capture the current header contract and mobile CSS baseline -> apply an iOS-safe minimum 16px control font size to prevent focus auto-zoom without disabling user pinch zoom -> contain dashboard overscroll and make header positioning unambiguous -> update the focused source-contract regression test -> run focused tests, type checking, and diff review.
Verify: `D:\PROJECT\manager_points\frontend :: npm test -- --run src/components/layout/Header.test.tsx` => header/mobile regression contract passes; `D:\PROJECT\manager_points\frontend :: npm run typecheck` => TypeScript passes; `D:\PROJECT\manager_points :: git diff --check` => patch formatting is valid.
Done: iOS focus on `input`, native `select`, and `textarea` no longer auto-zooms; deliberate pinch zoom remains available; dashboard content can scroll normally on mobile while the header does not drag with it; desktop layout and form behavior remain unchanged.
Gate: None
