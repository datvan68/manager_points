Task: open-dormitory-edit-modal-from-student-profile | feature_development | Risk: medium | Profile: Quick
Objective: Clicking the KTX detail icon on a student profile opens the editable `Sửa đơn đăng ký` form directly, with no `Chi tiết đăng ký Ký túc xá` modal or read-only intermediate state.
Boundary: `frontend/src/components/students/**` | Write: `frontend/src/components/students/StudentDormitoryCard.tsx`, `frontend/src/components/students/StudentDormitoryCard.test.tsx`
Targets: `StudentDormitoryCard` modal trigger, modal open/close and edit-form initialization state, dialog title/body/footer, existing staff/self-service update handlers, and focused component tests.
Steps: Inspect the current detail/edit transition and permission behavior -> initialize the existing complete edit form when the icon is clicked -> render it immediately under the `Sửa đơn đăng ký` title and remove the detail-only presentation/transition -> preserve cancellation, focus restoration, validation, save/error handling, refresh, and authorized API routing -> update focused regression tests.
Verify: `D:\PROJECT\manager_points\frontend` :: `npm test -- "src/components/students/StudentDormitoryCard.test.tsx"` => the icon opens `Sửa đơn đăng ký`, no detail modal is rendered, authorized edits still save, failures preserve input, and close restores focus; `D:\PROJECT\manager_points\frontend` :: `npm run typecheck` => no introduced TypeScript errors; `D:\PROJECT\manager_points` :: `git diff --check` => valid patch formatting.
Done: For a student with a linked KTX registration, the accessible KTX icon opens the complete editable form titled `Sửa đơn đăng ký` in one action; `Chi tiết đăng ký Ký túc xá` and its extra edit transition are absent; existing permissions, payload fields, success/error behavior, non-resident card state, room/price display, and registration-page modal remain unchanged.
Gate: None
