Task: attendance-rule-records-and-member-timeline-status | bug_fix | Risk: MEDIUM
Objective: Reconcile one training record per completed attendance-rule unit and return the signed-in member's attendance status correctly in the activity timeline.
Scope:
- backend/src/club-attendance/activity-completion.service.ts :: completion award reconciliation :: derive the earned record count as `floor(approved present-or-late attendances / minimum_attendance)` for every configured criterion, create each missing unit as a quantity-one AcademicRecord with deterministic sequence idempotency, preserve the legacy first award as sequence one, and deactivate surplus generated units when eligible attendance decreases.
- backend/src/club-attendance/club-attendance-sync.service.ts :: attendance-driven rule evaluation :: run completion reconciliation after approved attendance sync and after rejection or deletion so rule-based AcademicRecords match the current eligible attendance count without duplicating units on retries.
- backend/src/club-attendance/activity-completion.service.spec.ts :: rule accumulation regressions :: verify that a one-session rule produces three distinct records after three approved sessions, higher thresholds produce only complete units, multiple criteria are reconciled independently, retries are idempotent, legacy sequence one is reused, and attendance removal deactivates only surplus generated units.
- backend/src/club-attendance/club-attendance-sync.service.spec.ts :: sync integration regressions :: verify active-rule approval and revocation both trigger reconciliation while legacy per-attendance scoring remains unchanged when no active rule exists.
- backend/src/activity-schedules/activity-schedules.service.ts :: student timeline identity resolution :: resolve the authenticated JWT user ID to its linked Student ID before querying ActivityAttendance, while still accepting an explicit Student ID, so `my_attendance` is populated for the same member identity used by QR and proximity check-in.
- backend/src/activity-schedules/activity-schedules.service.spec.ts :: member timeline regressions :: verify user-linked and direct student identities return the correct schedule-level `my_attendance`, unrelated members remain hidden, and staff roster behavior is unchanged.
- docs/taskscope.md :: execution contract :: record the inspected causes, bounded implementation, verification, and acceptance criteria.
Out: Rule-management UI wording, criterion point values, manual academic records, attendance-session QR/GPS validation, staff roster presentation, database schema or index migrations, dependency upgrades, and unrelated files and behavior.
Context: The current completion service creates at most one AcademicRecord per activity, student, and criterion after the minimum threshold is reached, so a rule of one session never accumulates beyond one record. The existing award document can remain a compatibility marker for sequence one; additional units can be reconciled from deterministic AcademicRecord idempotency keys without changing indexes. The timeline service currently queries attendance with `requester.studentId || requester._id || requester.id`, but JWT authentication supplies `userId`; check-in resolves that user to the linked Student document before storing attendance, causing timeline lookups to miss the saved record. The activity page already reloads the timeline after successful QR or proximity check-in.
Steps:
1. Replace one-time completion awarding with exact count reconciliation per criterion, using the existing award-linked record as unit one and stable sequence keys for later units.
2. Invoke reconciliation from both approval and revocation paths so retries and attendance status changes converge on the same active record count.
3. Resolve student identity in the activity timeline using the Student model already registered by ActivitySchedulesModule, then query schedule attendance with the resolved Student ID.
4. Add focused backend regressions and review only the scoped diff for unintended behavior, localization, encoding, or line-ending changes.
Verify:
- backend :: npm test -- --runInBand club-attendance/activity-completion.service.spec.ts club-attendance/club-attendance-sync.service.spec.ts activity-schedules/activity-schedules.service.spec.ts => targeted Jest suites pass for accumulated records, reconciliation, and member timeline status.
- backend :: npm run build => NestJS TypeScript build completes without errors.
- repository root :: git diff --check -- backend/src/club-attendance/activity-completion.service.ts backend/src/club-attendance/club-attendance-sync.service.ts backend/src/club-attendance/activity-completion.service.spec.ts backend/src/club-attendance/club-attendance-sync.service.spec.ts backend/src/activity-schedules/activity-schedules.service.ts backend/src/activity-schedules/activity-schedules.service.spec.ts docs/taskscope.md => no whitespace errors in scoped files.
- repository root :: git status --short and scoped diff inspection => no unrelated or pre-existing user changes are overwritten, and localized files preserve their existing UTF-8 content and line-ending conventions.
Done:
- With one active criterion and `minimum_attendance = 1`, one member with three approved present-or-late attendance sessions has exactly three active quantity-one AcademicRecords for that criterion; re-evaluation creates no duplicates.
- For any positive minimum, active generated records per criterion equal the number of complete attendance units, and rejecting or deleting attendance removes only units no longer earned.
- A member authenticated by User ID receives `my_attendance.status = present` or `late` for the checked-in schedule, and “Lịch trình & dòng thời gian” no longer falls back to “Chưa điểm danh” after the existing post-check-in refresh.
- Targeted backend tests, backend build, and scoped diff checks pass.
Gate/Stop: Stop if existing production AcademicRecords cannot distinguish rule-generated units from manual or legacy records safely, or if reconciliation would require modifying records outside `source_type = activity_completion` for the same activity, student, semester, and criterion.
Rollback: Revert the completion reconciliation and timeline identity-resolution changes; reactivate only rule-generated records deactivated by the scoped reconciliation if rollback occurs after execution against persisted data.
Dependencies: Existing Student-to-User linkage, approved ActivityAttendance records, active ActivityCompletionRule configuration, and AcademicRecord idempotency/source fields. No new package, schema, index, or migration is required.
Artifacts: Final scoped diff plus targeted Jest and backend build output.
