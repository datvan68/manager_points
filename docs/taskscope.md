Task: schedule-member-attendance-modals | bug_fix | Risk: MEDIUM
Objective: Keep attendance-session management in the Attendance tab while active members check in directly from today's schedule through method-specific modals.
Scope:
- frontend/src/app/(dashboard)/activities/[activityId]/page.tsx :: attendance ownership and role routing :: mount one shared attendance-session state at page level, expose the Attendance tab only as the manager/president session-management surface, and route an active member's schedule action to the current session's GPS or QR modal without changing tabs or URL parameters.
- frontend/src/app/(dashboard)/activities/[activityId]/page.test.tsx :: end-to-end component regressions :: verify role-specific tab visibility, no member navigation to `tab=attendance`, GPS automatic submission and modal states, QR camera-modal opening, inactive-session handling, completed-state idempotency, and manager session controls.
- frontend/src/components/activities/ActivityScheduleTimeline.tsx :: today's attendance action contract :: pass the selected schedule to the attendance callback and render the self-check-in action only when the caller grants member check-in access, leaving roster display independent from the action.
- frontend/src/components/activities/ActivityScheduleTimeline.test.tsx :: timeline action regressions :: verify that only eligible members receive the action for today's schedule and that the selected schedule is returned without affecting administrator roster behavior.
- frontend/src/components/attendance/ProximityCheckinModal.tsx :: automatic GPS member flow :: add a modal that requests the member's current location once when opened, submits proximity check-in automatically, and presents locating, submitting, success, permission/range/API error, retry, and close states without a second attendance click.
- frontend/src/components/attendance/ProximityCheckinModal.test.tsx :: GPS modal regressions :: verify one-shot geolocation and submission, success feedback, retry behavior, error feedback, cleanup on close, and duplicate-submission prevention.
- docs/taskscope.md :: execution contract :: record the inspected behavior, bounded implementation, verification, and acceptance criteria.
Out: Attendance API contracts, server-side GPS distance validation, QR token generation or rotation, realtime transport or payloads, attendance completion/scoring rules, schedule CRUD, database migrations, dependency upgrades, the unused ActivityDetailWorkspace component, and unrelated files and behavior.
Context: The schedule action currently calls `handleTabChange('attendance')`, and `useAttendanceSession` is mounted only inside ActivityAttendanceTab. QrScannerModal already owns camera permission, scanning, success, and error UI. ProximityCheckinButton requires a second click after locating the member, so it does not satisfy the direct schedule flow. The existing attendance hook already receives session-open, session-close, and check-in events in realtime and must remain the single source of session and own-check-in state. A president may manage a session from the tab and self-check in from the schedule; an administrator or advisor without an active membership uses the tab only.
Steps:
1. Lift the attendance hook and member-modal state into ActivityDetailPage, derive the current member's own completed state by normalized Student ID, and pass the shared state and manager actions into ActivityAttendanceTab.
2. Restrict ActivityAttendanceTab to opening, displaying, and closing the active session plus the manager roster; remove QR and GPS self-check-in controls from that tab.
3. Change the timeline callback to identify the selected current-day schedule and invoke it only for an active member; retain the Attendance tab as the entry point for administrators, advisors, and presidents managing a session.
4. On a member schedule click, keep the current tab and URL unchanged; show an unavailable modal/message when no matching active session exists, open QrScannerModal for a QR session, or open ProximityCheckinModal for a proximity session.
5. In the GPS modal, acquire coordinates and call the existing proximity endpoint automatically exactly once per attempt, then show durable success or actionable error feedback. Reuse the existing QR scanner for camera access and token submission, and prevent either method from resubmitting an already completed check-in.
6. Refresh the activity timeline after success while retaining realtime session updates, add focused regressions, and inspect only the scoped diff for unintended localization, encoding, or user-owned changes.
Verify:
- frontend :: npm test -- "src/app/(dashboard)/activities/[activityId]/page.test.tsx" "src/components/activities/ActivityScheduleTimeline.test.tsx" "src/components/attendance/ProximityCheckinModal.test.tsx" => all targeted Vitest suites pass for manager-only tab behavior and direct member GPS/QR modal flows.
- frontend :: npm run typecheck => TypeScript completes without errors.
- repository root :: git diff --check -- "frontend/src/app/(dashboard)/activities/[activityId]/page.tsx" "frontend/src/app/(dashboard)/activities/[activityId]/page.test.tsx" frontend/src/components/activities/ActivityScheduleTimeline.tsx frontend/src/components/activities/ActivityScheduleTimeline.test.tsx frontend/src/components/attendance/ProximityCheckinModal.tsx frontend/src/components/attendance/ProximityCheckinModal.test.tsx docs/taskscope.md => no whitespace errors in scoped files.
- repository root :: git status --short and scoped diff inspection => no unrelated or pre-existing user changes are overwritten, and localized files retain their existing encoding and line-ending conventions.
Done:
- The Attendance tab is visible only to accounts that can manage attendance and contains session opening/management controls, not member self-check-in controls.
- An eligible active member clicking `Điểm danh` on today's schedule remains on the current tab and receives a modal based on the active session method.
- A proximity session requests location and submits once automatically, then the modal reports success or a recoverable permission, location, range, or API failure without requiring a second attendance click.
- A QR session opens the existing camera scanner modal and records the scanned token through the existing check-in path.
- Existing own attendance, a repeated click, or a realtime confirmation cannot create a duplicate client submission; successful GPS or QR attendance refreshes the displayed schedule state.
- Targeted tests, frontend type-check, and scoped diff checks pass.
Gate/Stop: Stop if member session discovery requires a backend contract change, if an active attendance session cannot be matched safely to the selected current-day schedule, if browser geolocation cannot be initiated from the schedule-click interaction, or if implementation requires files outside Scope.
Rollback: Revert only the scoped page routing/state changes, timeline callback changes, new GPS modal, and their tests; no data rollback is required.
Dependencies: Existing useAttendanceSession realtime state, attendanceSessionApi QR/proximity endpoints, browser Geolocation API, QrScannerModal camera flow, and the current dialog/toast providers. No new package or migration.
Artifacts: Final scoped diff plus successful targeted Vitest and frontend type-check output.
