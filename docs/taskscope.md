Task: activity-detail-attendance-teacher-ui | bug_fix | Risk: medium | Profile: Quick
Objective: Show the Attendance tab and own-class manual attendance controls to an eligible teacher, and reorganize the Admin/advisor Attendance UI into a clear responsive primary/secondary layout.
Boundary: `frontend/src/app/(dashboard)/activities/[activityId]/**` | Write: `frontend/src/app/(dashboard)/activities/[activityId]/page.tsx`, `frontend/src/app/(dashboard)/activities/[activityId]/page.test.tsx`
Targets: `canManageAttendance`, `allowedStudentTabs`, `displayedTab`, Attendance navigation, `ActivityAttendanceTab`
Steps: Reproduce the capability-versus-tab mismatch -> allow `attendance` when backend `effective_methods` is non-empty -> keep teacher methods/classes capability-driven -> place session status/opening, manual-class controls, and active-session content in the primary area -> place `AttendanceGrantManager` in a secondary Admin/advisor panel, using two columns on desktop and session-first stacking on mobile -> add focused role/layout regressions.
Verify: `D:\PROJECT\manager_points\frontend` :: `npm test -- "src/app/(dashboard)/activities/[activityId]/page.test.tsx"` => teacher with `manual_class` capability sees Attendance and only own-class manual actions; teacher does not see grant administration or ungranted QR/GPS; Admin/advisor sees session controls before a separate grant panel; existing student restrictions pass. `D:\PROJECT\manager_points` :: `git diff --check` => valid scoped diff.
Done: Eligible teacher can enter the Attendance tab and open manual attendance only for classes returned by capabilities. Admin/advisor desktop layout is primary attendance content plus permission sidebar; mobile stacks attendance before permissions. Existing backend authorization, active-session UI, student check-in, and realtime behavior are unchanged.
Gate: None
