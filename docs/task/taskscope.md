task: "Refine dashboard leaderboard, attention KPI, and recent record names"
pipeline: bug_fix
profile: Full
objective: "Show five leaderboard rows at a time with scrolling, remove the evaluation-period badge, count all in-scope students with more than three discipline occurrences, and display human-readable recent academic record names."

evidence:
  current_behavior: "frontend/src/components/dashboard/StudentSpotlightPanel.tsx renders up to 10 rows in a 620/760px viewport; DashboardHeader.tsx renders `Đợt: <status>`; backend/src/system/system.service.ts omits `kpis.studentAttentionCount`; AttendanceRecordPanel.tsx displays `record_title`, while academic-record.service.ts stores single-option titles as `Lựa chọn option <id>` despite persisting `selected_option_label`."
  expected_behavior: "Each leaderboard retains up to 10 API entries but exposes a five-row-height scroll viewport; the period badge is absent; the admin attention KPI equals the number of scoped students whose normalized discipline occurrence total is >3; recent single-option records show their option label instead of an internal ID."
  root_cause: "The leaderboard viewport was enlarged with its 10-row cap; period status is rendered unconditionally when activePeriod exists; dashboard API never returns studentAttentionCount and current discipline grouping counts documents instead of normalized quantity; recent-record UI prioritizes the technical record_title generated by the single-option write path."

scope:
  inspect: ["frontend/src/app/(dashboard)/page.tsx:DashboardPage header/metrics wiring", "backend/src/system/system.service.ts:getDashboardMetrics role and semester filters"]
  write: ["frontend/src/components/dashboard/StudentSpotlightPanel.tsx:leaderboard viewport", "frontend/src/components/dashboard/DashboardHeader.tsx:period badge", "frontend/src/components/dashboard/AttendanceRecordPanel.tsx:record title selection", "frontend/src/app/(dashboard)/page.test.tsx:dashboard UI regression assertions", "backend/src/system/system.service.ts:getDashboardMetrics", "backend/src/system/system.service.spec.ts:dashboard KPI regression", "backend/src/academic-record/academic-record.service.ts:single-option record title", "backend/src/academic-record/academic-record.service.spec.ts:single-option title regression"]
  preserve: ["Leaderboard API cap of 10, tab order, ranking order, and empty/mobile states", "semester selector, refresh control, activePeriod data used by other dashboard logic", "teacher KPI remains `Hồ sơ chờ phê duyệt`", "RBAC, semester/class scope, API response shape except populating the existing studentAttentionCount field", "legacy records require no migration"]
  out: ["Changing evaluation-period workflow", "Changing the five-item recent-academic-record limit", "Database migration or rewriting historical records", "Unrelated dashboard panels"]

acceptance_criteria:
  - "AC-01: Every leaderboard tab can contain up to 10 ranked entries, shows approximately five rows without scrolling, and exposes entries 6-10 through vertical scroll."
  - "AC-02: The dashboard header no longer renders the `Đợt: ...` badge at any period status; semester selection and refresh remain available."
  - "AC-03: For admin scope, `Sinh viên cần xử lý` equals the count of distinct students with a normalized discipline total >3 across the full scoped semester dataset, including quantity values and students outside the top 10; exactly three is excluded. Teacher approval KPI is unchanged."
  - "AC-04: Recent single-option academic records show `selected_option_label` (with a safe criterion/title fallback) and never expose `Lựa chọn option <id>` when a label exists; new/updated records store a human-readable option label without changing structured option fields."

execution:
  - "E-01 [AC-01] frontend/src/components/dashboard/StudentSpotlightPanel.tsx → keep the 10-entry render cap and reduce the overflow container to a five-row viewport."
  - "E-02 [AC-02] frontend/src/components/dashboard/DashboardHeader.tsx → remove the active-period badge, its formatter/color helpers, and unused icon/prop only if no remaining consumer requires them."
  - "E-03 [AC-03] backend/src/system/system.service.ts:getDashboardMetrics → add a quantity-normalized, role/semester-scoped discipline aggregation that counts all groups above three before the leaderboard limit; return studentAttentionCount in kpis."
  - "E-04 [AC-04] frontend/src/components/dashboard/AttendanceRecordPanel.tsx and backend/src/academic-record/academic-record.service.ts → prefer/store the option label while retaining fallbacks for legacy rows."
  - "E-05 [AC-01,AC-02,AC-04] frontend/src/app/(dashboard)/page.test.tsx → assert the five-row scroll contract, removed badge, and label precedence."
  - "E-06 [AC-03,AC-04] backend/src/system/system.service.spec.ts and backend/src/academic-record/academic-record.service.spec.ts → cover threshold, quantity, top-10 independence, scoped filters, and option-label persistence."

temporary_artifacts:
  create: []
  cleanup: []
  retain: ["docs/task/taskscope.md: user-requested rolling taskscope"]

verification:
  - "V-01 [AC-01,AC-02,AC-04] npm --prefix frontend test -- src/app/(dashboard)/page.test.tsx → focused dashboard tests pass."
  - "V-02 [AC-03] npm --prefix backend test -- system/system.service.spec.ts --runInBand → dashboard KPI tests pass."
  - "V-03 [AC-04] npm --prefix backend test -- academic-record/academic-record.service.spec.ts --runInBand → single-option record tests pass."
  - "V-04 [AC-01,AC-02,AC-04] npm --prefix frontend run typecheck → no TypeScript errors."
  - "V-05 [AC-03,AC-04] npm --prefix backend run build → NestJS build succeeds."
  - "V-06 [AC-01,AC-02,AC-03,AC-04] Manual dashboard fixture with 11 ranked students, quantities 3/4, and a labeled option record → five visible rows plus scroll, no period badge, correct >3 KPI, readable recent title."

risks: ["The attention count requires an additional aggregation unless combined with the discipline pipeline; preserve existing request latency and scoped filters.", "Historical rows without selected_option_label must use a non-ID fallback without data mutation."]
stop_conditions: ["Stop if the requested count must use a definition other than normalized discipline occurrences in the selected semester.", "Stop if removing the badge also requires removing activePeriod from the public dashboard contract.", "Stop if accurate legacy labels require a persistent-data migration."]
